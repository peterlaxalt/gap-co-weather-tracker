# No weather API key needed — this app uses the free, keyless Open-Meteo API.

# Optional: the calendar date your trip starts (YYYY-MM-DD). When set, forecast
# days are labeled "Day 1", "Day 2", etc. and that day's start/end towns are
# highlighted. Leave unset to just label by calendar date.
TRIP_START_DATE=
